<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://www.jahangeer.com/wp-content/uploads/2014/12/2400x780_Movie2014Banner.jpg)">
  <h1>Info of <%= @list.name %> </h1>
</div>

<div class="flash flash-success">
  <ul>
    <% @list.movies.each do |movie| %>
      <p> <%= movie.title %> </p>
    <% end %>
  </ul>
</div>

<%= link_to "Create Bookmarks", new_list_bookmark_path(@list), class:"btn-flat" %>
<%= link_to "Go back to the home page", lists_path, class:"btn-flat" %>
