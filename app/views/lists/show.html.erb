<h1>Info of <%= @list.name %> </h1>

<ul>
  <% @list.movies.each do |movie| %>
    <p> <%= movie.title %> </p>
  <% end %>
</ul>

<%= link_to "Create Bookmarks", new_list_bookmark_path(@list) %>
<%= link_to "Go back to the home page", lists_path %>
